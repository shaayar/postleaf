import { Column } from "@once-ui-system/core";
import { StylePanel } from "@once-ui-system/core";

export default function Settings() {
  return (
    <Column fillWidth horizontal="center" paddingTop="40" paddingX="l">
      <StylePanel maxWidth="s"/>
    </Column>
  );
}